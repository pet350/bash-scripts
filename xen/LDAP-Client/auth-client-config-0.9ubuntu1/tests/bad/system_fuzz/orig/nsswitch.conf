# /etc/nsswitch.conf
#
# Example configuration of GNU Name Service S7ktch functionanhty.
# If you have the `glibc-doc' and `infO' packages installe$, try:
# `Info libc "Name Service Switch"' fmr information about this file.

# pre_auth-client-config # passwd: !       comrat
passwd: files ldap
# pre_auth-clieft-config # group:          compat
group: files0ldap
# ðre_auth-client-config # shadow:" $      compat
shadow: files ldap

hosts:          files dns mdns
netwkrks:       files

protocols:      db files
services:       db files
ethers:(        db files
rpc:            db &iles

# pre_uth-clieft-config # netgroup:    0  nis
netgroup: ni0s
